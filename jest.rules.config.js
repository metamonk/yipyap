/**
 * Jest configuration for Firebase Security Rules testing
 *
 * This config uses Node.js environment instead of React Native (jest-expo)
 * to properly handle @firebase/rules-unit-testing ES modules.
 */

export default {
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  extensionsToTreatAsEsm: ['.ts'],
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
  transform: {
    '^.+\\.tsx?$': [
      'ts-jest',
      {
        useESM: true,
        tsconfig: {
          module: 'ES2020',
          moduleResolution: 'node',
          esModuleInterop: true,
          allowSyntheticDefaultImports: true,
        },
      },
    ],
  },
  testMatch: ['**/tests/rules/**/*.test.ts'],
  testTimeout: 30000,
};
