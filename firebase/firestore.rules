rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all access initially
    // Will be updated in Story 1.5 with proper authentication rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
