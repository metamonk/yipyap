rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny all access initially
    // Will be updated in Story 2.2 for profile photos with proper authentication
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
